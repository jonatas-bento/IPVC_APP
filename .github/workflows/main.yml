name: Build Application

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  setup-android:
    runs-on: ubuntu-latest
    steps:
    - name: Install Android SDK and Accept Licenses
      run: |
        wget https://dl.google.com/android/repository/commandlinetools-linux-7583922_latest.zip
        unzip commandlinetools-linux-7583922_latest.zip -d ${HOME}/android-sdk
        export ANDROID_HOME=${HOME}/android-sdk
        export PATH=$PATH:${ANDROID_HOME}/cmdline-tools/latest/bin
        yes | sdkmanager --licenses
        sdkmanager "platform-tools" "platforms;android-30" "build-tools;30.0.3"
      shell: bash


    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v1
      with:
        dotnet-version: '7.0.x'

    - name: Accept Android SDK licenses
      run: yes | sdkmanager --licenses

    - name: Build Project
      run: dotnet build --configuration Release --no-restore /p:AndroidSdkDirectory=$HOME/android-sdk

    - name: Publish Project
      run: dotnet publish -c Release -o ./output --no-build

    - name: Upload Build Artifacts
      uses: actions/upload-artifact@v4
      with:
        name: android-app
        path: output/**/*.apk

    - name: Clean Up Build Files
      run: rm -rf ./output

